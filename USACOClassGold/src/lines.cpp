#include <iostream>
#include <set>
using namespace std;

int N;
const int MAXN = 201;
int pointsX[MAXN];
int pointsY[MAXN];
int solve(){
    for(int i = 0; i < N; i )
}
int main(int argc, const char** argv) {
    cin >> N;
    for(int i = 0; i < N; i ++){
        cin >> pointsX[i] >> pointsY[i];
    }
    return 0;
}